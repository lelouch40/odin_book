<%=form_for(current_user.friendships.build(:friend_id => params[:friend_id], :user_id=> current_user, accepted: false)) do |f|%>
   <%= f.button  :Submit , name: "friendships",value: "create"%>
   <%= f.button  :Ignore%>
	<%end%>