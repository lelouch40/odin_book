<%@user.each do |u| %>
  <%#if u.avatar.present?%>
  <%#=image_tag(u.avatar.url) %>
  <%#end%>
  <%if u.avatar.present?%>
  <%=image_tag(u.avatar.url) %>
  <%end%>
<%=link_to(u.username, :action=>"show",:id=>u) %>
<%=link_to("Edit", :controller=>"users",:action=>"edit",:id=>u) %>
<%end%>
